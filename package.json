{
  "name": "death-rejected",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "tsc --noEmit && vite build && node tests/logic-smoke.mjs"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "@types/node": "22.19.19",
    "@types/three": "0.180.0",
    "typescript": "5.9.3",
    "vite": "8.0.13"
  }
}
